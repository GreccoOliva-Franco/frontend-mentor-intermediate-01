"use client";

export default function CheckOut() {
  return (
    <button
      onClick={() => {
        alert("Functionality not implemented (out of scope)");
      }}
      className="w-full p-4 rounded-lg bg-button-bg text-button-text font-bold"
    >
      Checkout
    </button>
  );
}
